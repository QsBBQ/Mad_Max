<h2>Welcome to IPAM</h2>
  <p>Add search bar</p>
  <section>
    <!--Would love some advice how to handle this better
    I have searched a ton on lists and tables and collapsing expanding items
    Tried with css and javascript examples but all came out wonky.
    Haven't tried the bootstrap one but may if I don't find something.
    -->
    <strong>Dystopian Locations</strong>
    <ul>
      <% locations.each do |location| %>
        <li><%= location.name %></li>
          <ul class="accordion">
            <% location.subnets.each do |subnet| %>
              <li id="subnet"><strong><%= subnet.network %></strong></li>
                <table id="subnet_table">
                    <% subnet.ip_addresses.each do |ip| %>
                    <tr>
                      <th>IP Address</th>
                      <th>DNS Name</th>
                      <th>Status</th>
                      <th>Comments</th>
                    </tr>
                    <tr>
                      <td><%= ip.address %></td>
                      <td><%= ip.dns_name %></td>
                      <td><%= ip.status %></td>
                      <td><%= ip.comments %></td>
                    </tr>
                  <% end %>
                </table>
            <% end %>
          </ul>
      <% end %>
    </ul>
  </section>
